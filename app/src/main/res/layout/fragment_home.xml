<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:fillViewport="true"
    tools:context=".fragment.HomeFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:paddingBottom="96dp"
        android:clipToPadding="false"
        android:clipChildren="false">

        <!-- ✨ Citation motivante -->
        <TextView
            android:id="@+id/text_motivation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Chaque petite action compte ✨"
            android:textSize="24sp"
            android:textColor="@color/textPrimary"
            android:fontFamily="sans-serif-medium"
            android:textAlignment="center"
            android:gravity="center"
            android:lineSpacingExtra="8dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="32dp" />

        <!-- ✅ Pourcentage de complétion -->
        <TextView
            android:id="@+id/text_progress_percentage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0%"
            android:textSize="16sp"
            android:textColor="@color/textPrimary"
            android:textStyle="bold"
            android:layout_gravity="end"
            android:layout_marginBottom="8dp" />

        <!-- ✅ Barre de progression -->
        <ProgressBar
            android:id="@+id/progress_day"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="16dp"
            android:max="100"
            android:progress="0"
            android:progressDrawable="@drawable/glow_progress"
            android:layout_marginBottom="28dp" />

        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:layout_marginTop="8dp"
            app:cardElevation="2dp"
            app:cardBackgroundColor="@color/white">

            <com.google.android.material.tabs.TabLayout
                android:id="@+id/tab_layout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:tabIndicatorColor="@color/primaryAccent"
                app:tabIndicatorHeight="4dp"
                app:tabSelectedTextColor="@color/primaryAccent"
                app:tabTextColor="@color/textSecondary"
                app:tabIndicatorFullWidth="false"
                app:tabRippleColor="@android:color/transparent"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:background="@android:color/transparent" />
        </com.google.android.material.card.MaterialCardView>


        <!-- 📋 Liste dynamique -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_habits"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false"
            android:layout_marginBottom="16dp" />

        <!-- 🌱 Message vide -->
        <TextView
            android:id="@+id/text_empty_today"
            style="@style/TextAppearance_EmptyState"
            android:text="Aucune habitude à afficher 😴"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_marginBottom="32dp" />

    </LinearLayout>
</androidx.core.widget.NestedScrollView>
