<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/habitCompactRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="64dp"
    android:orientation="horizontal"
    android:paddingHorizontal="20dp"
    android:paddingVertical="12dp"
    android:gravity="center_vertical"
    android:background="?attr/selectableItemBackground">

    <!-- 🎯 Emoji -->
    <TextView
        android:id="@+id/habitIconCompact"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:gravity="center"
        android:text="📚"
        android:textSize="20sp"
        android:textStyle="bold"
        android:background="@drawable/bg_circle"
        android:textColor="@android:color/white" />

    <!-- 📋 Détails -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="14dp"
        android:lineSpacingExtra="2dp">

        <TextView
            android:id="@+id/habitNameCompact"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Nom de l’habitude"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/textPrimary"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/text_last_planned"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Dernier jour prévu : Lundi"
            android:textSize="12sp"
            android:textColor="@color/textSecondary"
            android:visibility="gone"
            android:layout_marginTop="2dp" />

        <TextView
            android:id="@+id/text_missed_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Non validée depuis 3 jours"
            android:textSize="12sp"
            android:textColor="@color/red"
            android:layout_marginTop="2dp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/text_routine_name_compact"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Routine : Matin"
            android:textSize="12sp"
            android:textColor="@color/textSecondary"
            android:layout_marginTop="2dp"
            android:visibility="visible" />
    </LinearLayout>

    <!-- ☑️ Check (mode édition uniquement) -->
    <CheckBox
        android:id="@+id/habitCheckCompact"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone" />

    <!-- 🎨 Point couleur -->
    <View
        android:id="@+id/habitIconCompactWrapper"
        android:layout_width="12dp"
        android:layout_height="12dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="4dp"
        android:background="@drawable/bg_circle"
        android:visibility="visible" />
</LinearLayout>
