<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingHorizontal="20dp"
    android:paddingVertical="14dp"
    android:gravity="center_vertical"
    android:background="@color/cardBackground"
    android:elevation="2dp">

    <TextView
        android:id="@+id/routineNameTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Routine Matin"
        android:textColor="@color/textPrimary"
        android:textSize="17sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end" />

    <!-- ✏️ Édition (optionnel, si tu veux rajouter plus tard un bouton ou icône) -->
    <!--
    <ImageButton
        android:id="@+id/buttonEditRoutine"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_edit"
        android:contentDescription="Modifier la routine"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:tint="@color/primaryAccent" />
    -->
</LinearLayout>
